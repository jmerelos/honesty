<p>
	<h1 class="center"><%= @book.name %></h1>
	<h6 class="center">By: <%= @book.author.name %></h6>	
	<hr/>
</p>
<div class="row">
	<div class="well col-md-10 col-md-offset-1">	
		<%= simple_format(@book.description) %>
	</div>
</div>
<% if logged_in? %>
<%= link_to "Edit this Book", edit_book_path(@book), class: "btn btn-success pull-right" %>
<% end %>
