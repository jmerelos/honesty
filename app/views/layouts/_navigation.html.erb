<header class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <div class="nav_item"><%= link_to "Home", home_path %></div>
     <div class="nav_item"><%= link_to "Books Listing", books_path %></div>    
    <% if logged_in? %>
      <div class="nav_item">
        <%= link_to "Add Book", new_book_path %>
      </div> 
      <div class="nav_item dropdown pull-right">
        <ul class="dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
          <%= link_to current_user.name %>
          <span class="caret"></span>          
        </ul>
        <ul class="dropdown-menu">
          <li><%= link_to "Logout", logout_path %></li>
        </ul>
    </div>

    <% else %>
      <div class="nav_item">
        <%= link_to "Login", login_path %>
      </div>
    <% end %>
  </div>
</header>
